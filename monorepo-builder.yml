parameters:
  # for "merge" command
  data_to_append:
    require-dev:
      symplify/monorepo-builder: '^4.7'

  # for "split" command
  directories_to_repositories:
    packages/Core: 'git@github.com:BeApp/beapp.library.pusher-core.web.git'
    packages/ClientFirebase: 'git@github.com:BeApp/beapp.library.pusher-client-firebase.web.git'
    packages/ClientRabbitMQ: 'git@github.com:BeApp/beapp.library.pusher-client-rabbitmq.web.git'
